---
name: Auto Assign

on:
  pull_request:
    types:
      - opened
  issues:
    types:
      - opened

permissions:
  issues: write
  pull-requests: write

jobs:
  auto-assign:
    name: Auto-assign to author
    runs-on: ubuntu-latest
    steps:
      - name: 👤 Assign PR or issue to its author
        uses: toshimaru/auto-author-assign@v3.0.1
